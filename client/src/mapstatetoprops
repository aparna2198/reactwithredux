
/*mapStateToProps 
this say we going to take our state object(all the data thast inside of redux store)
run some calculation on it or some computation thats going to cause that data to 
eventually shpow uo as props inside of our component 



called with all of the state inside of the redux store so thast 
state is going to be isSignedIn here which is called using auth object 
auth which is gapi.auth2.getAuthInstance.
there 
state bascialy means something that changes

return {isSignedIn :state.auth.isSignedIn}

therfore mapstaetoprop is to passon the data from store(store which consists of all the reducers)
down to the component which state argument
 */